# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 学生（大学物理课程的初学者，常见为大一理工科）：需要清晰的下一步、项目化练习和可复核的实验记录，期望系统在没有真实学伴的情况下也给出可信反馈。
- 教师（同课程任教老师，兼教学班负责人）：需要班级概况、风险信号、课程项目矩阵、人工复核入口，期望保留对 AI 建议分的最终判断权。

## Product Purpose

让师生在不接入真实教务系统或大模型的前提下，仍能跑通"诊断 → 项目匹配 → 任务闯关 → 虚拟实验 → 学习报告"的完整闭环，让每一次学习行动留下可解释证据，并让教师从班级层面识别风险并人工复核。

成功意味着：
- 学生首次会话 5 分钟内能完成诊断并看到第一个项目匹配；
- 教师能在 1 分钟内定位需要关注的学生并完成一次复核；
- 所有过程数据本地持久化、跨刷新不丢，可被撤销。

## Positioning

教学逻辑由"过程证据"而非"对错分数"驱动——AI 只生成建议分、提示和参考材料，所有破坏性动作（切换项目、重新诊断、重置记录）都有二次确认和 24 小时 可恢复快照。这是周边 SaaS 教务系统或通用 AI 助教都没有承诺的一点。

## Operating Context

- 浏览器端仿真工作台，运行时只读写 localStorage；
- 内置 1 个学生账号和 1 个教师账号作为演示登录（密码统一为 dunzhongwan）；
- 演示班级固定为"物理学 2601 班"，演示学生"陈思远"，演示教师"顿老师"；
- 所有题目、项目、能力维度、教案均为内置数据，可由教师新增/编辑本地项目；
- 真实部署依赖：客户端浏览器 + 静态托管，无需后端、无需大模型密钥。

## Capabilities and Constraints

- 当前为本地演示版，不连接真实教务系统、数据库或远程大模型；
- 完整覆盖诊断（8 题，4 维度）、项目匹配、五阶任务闯关、超声波测距虚拟实验、学习报告、教师班级画像与评价复核、教师课程项目矩阵、教学洞察；
- "四阶能力"模型（知识理解 / 专业应用 / 创新实践 / 团队协作）是产品内定的评价维度，不暴露给用户编辑；
- 浏览器为唯一运行载体，不提供原生 iOS/Android 适配；
- 国际化当前仅中文（zh-CN）；
- 教师端 AI 洞察与项目推荐基于本地启发式规则，不接入外部 API。

## Brand Commitments

- 校名：长江大学文理学院（仅在登录页与 sticky 上下文条显示，不进入其他文案）；
- 系统名：大学物理 AI 智慧教学系统 / 大学物理 AI 智慧教学工作台；
- 主色：天蓝（#0369a1，学术理性）+ 灰白背景，避免营销式饱和配色；
- 字体：苹方（PingFang SC）/ 微软雅黑（Microsoft YaHei）优先，西文 fallback 到系统 sans；
- Logo：用户提供的长江大学文理学院 PNG（/assets/college-brand.png），侧栏与登录页共用；
- 视觉规则：浅色学术工作台，紧凑但留有层级，所有重要操作配中文动作标签。

## Evidence on Hand

- 设计基准：DESIGN.md（用户最初确认的视觉方向）；
- 完整 critique 快照：.impeccable/critique/2026-08-18T10-17-48Z__src-components-physics-app-tsx.md（双代理评估，Score 19/40 Poor）；
- 截图能力：通过 ego-browser 实测 Lab 超声脉冲-回波时域 SVG、Sidebar 分组导航、Sticky 上下文条、Sheet 抽屉均已渲染正确；
- 仿真数学：超声测距的波形、时间序列与误差模型见 src/lib/physics.ts 中的 simulateUltrasonicMeasurement / simulateUltrasonicWaveform；
- 学生与教师 demo 账号：用户名 student / teacher，密码 dunzhongwan；
- 不存在的证据（未来工作不得编造）：真实学生成绩、真实班级数据、真实教学评估、真实大模型推理日志。

## Product Principles

1. **过程证据 > 最终分数**——产品价值在于留下了什么可解释证据，而非 AI 给学生打了多少分。
2. **破坏性动作必须可逆**——任何会清空学生过程数据（选项目、重新诊断、重置记录）的动作都必须有二次确认和 24 小时可恢复快照。
3. **教师保留最终判断权**——AI 只产出建议分，教师复核结果始终覆盖之；这一边界在 UI 与文案上始终可见。
4. **本地优先、可离线工作**——所有数据本地持久化，不依赖网络或外部大模型；这样既保护隐私，也让学生在课堂断网时仍可继续。
5. **一次只问一件事**——界面层级坚持一个主任务 + 一个次任务，不让学生/教师同时面对超过 4 个并排决策点。

## Accessibility & Inclusion

- 中文界面（zh-CN），系统字体栈优先苹方/微软雅黑，弱网与离线场景下仍可用；
- 所有破坏性动作走 AlertDialog + role=alert / role=status 通知；
- Sidebar 导航配 aria-current=page、可发现按钮带 aria-label；
- WCAG AA 对比度目标：正文 ≥ 4.5:1，大字 ≥ 3:1；
- 已知不足：图表（BarChart / Lab 波形）当前无 accessible 数据表 fallback，未来工作可补。
