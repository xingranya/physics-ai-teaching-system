# 风险评估

- 本地存储损坏：已通过字段归一化和初始种子恢复，旧裸 JSON 也可迁移。
- 客户端门禁：明确标注为演示账号，不把它当作真实认证。
- 移动端信息密度：侧栏使用 offcanvas 抽屉，表格区域允许横向滚动。
- 图表依赖：固定到 Recharts 2.x，避免 Creative Tim 图表封装与 3.x 类型契约冲突。
- 外部图片资源：登录背景和 Logo 已复制到 `public/assets`，运行时不依赖远程站点。
